<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><input type="checkbox">
    <%= link_to task.title, task_path(task), class: "text-decoration-none" %></li>
  <% end %>
</ul>
<%= link_to "Create new task", new_task_path, class: "text-decoration-none" %>
